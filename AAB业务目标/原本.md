背景：在企业级数据中心(如教育、医疗行业场景)，运维人员常需处理复杂的 OS 问题(如：僵尸进程堆积、磁盘 I/O 异常、配置文件漂移)。传统运维依赖复杂的脚本和监控告警，门槛极高。

场景提炼：模拟一个真实的 Linux 生产节点，Agent 需应对管理员的自然语言诉求。

例如：当管理员说“帮我清理系统垃圾”时，Agent 必须通过环境感知发现是大日志文件占满了空间，但在执行删除前，必须触发“安全校验”，识别该文件是否为关键数据库日志，并评估当前权限是否合规，从而避免引发系统崩溃。