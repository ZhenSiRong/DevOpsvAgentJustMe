# 项目文档校对报告

> 校对日期：2026-04-30
> 校对范围：`devops-agent/README.md` + `devops-agent/docs/` + `docs-planning/`

---

## 总体评价

**发现 11 处不一致**，其中 2 处严重、5 处中等、4 处轻微。问题主要集中在两方面：

1. **前端技术栈**：多份规划层文档统一声称使用「Vue 3」，实际代码为 **React 18**
2. **目录结构描述**：主 README.md 的目录结构完全过时，是 Phase 1 时期的骨架，与当前代码严重不符

---

## 一、严重问题（影响信息正确性）

### 1.1 `docs-planning/06-tech-stack.md` — 前端选型错误

| 位置 | 文档描述 | 实际代码 |
|------|---------|---------|
| 第 11 行 | 表现层：**Vue 3** + Vite + Tailwind CSS | `frontend/package.json`：**React 18** + Vite + Tailwind CSS |
| 第 17-35 行 | 组件实现建议全部用 Vue 术语撰写（`v-for`、`ref()`、`computed()`、`reactive()`） | 前端使用 React hooks（`useState`、`useEffect`） |
| 第 39 行 | 备选方案：React 18 | React 实际上已是主选方案 |

**建议**：整份文档需要用 React 18 技术栈重写「表现层」部分。

### 1.2 `devops-agent/README.md` — 目录结构全部过时

README 第 5-93 行描述的目录结构与实际代码存在大量差异：

| README 声称 | 实际存在 | 状态 |
|-------------|---------|------|
| `api/deps.py` | ❌ 不存在 | 文件类型全部错误 |
| `api/router_chat.py` | `api/routes/chat.py` | 路径错误 |
| `api/router_probe.py` | `api/routes/probe.py` | 路径错误 |
| `api/router_sessions.py` | `api/routes/sessions.py` | 路径错误 |
| `api/router_audit.py` | `api/routes/audit.py` | 路径错误 |
| `api/router_auth.py` | ❌ 不存在 | 文件不存在 |
| `schemas/request.py` 等 | `api/schemas.py` | 位置/文件名错误 |
| `agent/context.py` | ❌ 不存在 | 未实现 |
| `tools/tool_system_probe.py` 等 | `tools/builtin/` 下不同文件名 | 完全重构 |
| `safety/anti_injection.py` | `safety/prompt_injection.py` | 文件名错误 |
| `tests/test_api_chat.py` 等 | 不同测试文件 | 文件名错误 |
| `scripts/create_user.py` | ❌ 不存在 | 文件不存在 |

**同时缺失**以下已存在的模块：
- `api/routes/orchestrator.py`（DAG 编排引擎）
- `api/routes/mcp.py`（MCP Server 管理）
- `api/routes/tools.py`（动态工具管理）
- `api/routes/config.py`（系统配置）
- `mcp/`（MCP 协议实现）
- `orchestrator/`（DAG 编排引擎核心）
- `tools/dynamic.py`（动态工具分发器）
- `tools/registry.py`（工具注册中心）

**建议**：根据当前代码完全重写目录结构章节。

---

## 二、中等问题（影响功能理解）

### 2.1 `docs-planning/07-architecture.mmd` — 架构图前端标注错误

Mermaid 架构图中标注浏览器使用「Vue 3 + Vite」（第 8 行），应改为「React 18 + Vite」。

### 2.2 `docs-planning/README.md` — 前端技术栈索引错误

第 64 行速查表中「表现层：Vue 3 + Vite + Tailwind CSS」应改为「React 18 + Vite + Tailwind CSS」。

### 2.3 `devops-agent/docs/backend-gap-analysis.md` — 大量功能已实现但标记未完成

| 原标记 | 功能 | 现状 |
|--------|------|------|
| ❌ P0-2 | LLM 流式响应（SSE） | ✅ 已实现 |
| ❌ P0-3 | MCP 工具插件化框架 | ✅ 已实现 |
| ❌ P0-4 | Agent 记忆/上下文管理 | ✅ 已实现 |

同时缺失以下已完成功能的记录：
- DAG 任务编排引擎（orchestrator/）
- 动态 MCP Server 注册管理
- 系统配置管理 API
- 执行命令重试机制

**建议**：更新状态列，新增 Phase 3/DAG 等已完成条目。

### 2.4 `devops-agent/README.md` — 快速开始命令过时

第 113 行 `python -m scripts.init_db` 应改为 `python scripts/init_db.py`。

### 2.5 `docs-planning/05-api-contract.md` — 缺失新增接口

文档描述「5 组 RESTful API」，实际已有 12 个路由模块、50+ 端点。新增但未记录的接口包括：
- `/api/v1/orchestrator/*`（DAG 编排）
- `/api/v1/mcp/*`（MCP Server 管理）
- `/api/v1/config/*`（系统配置）

---

## 三、轻微问题（格式/细节）

### 3.1 `devops-agent/docs/README.md` — 未列入新增文档

文档清单缺失 `.github/workflows/ci-cd.yml`、`Makefile`、`scripts/deploy.sh` 等 CI/CD 文件说明。

### 3.2 `docs-planning/08-development-guide.md` — 路由数量过时

第 36 行写「路由模块（10 个）」，实际 `api/routes/` 下已有 12 个路由文件（含 orchestration、config、mcp 等）。建议核实并更新。

### 3.3 `docs-planning/README.md` — 更新时间标签过时

第 85 行标注「最后更新：2026-04-27」，实际 08-development-guide.md 已更新到 04-29。

### 3.4 `.gitignore` — 缺失 CI/CD 产物

`devops-agent/.gitignore` 未包含 `coverage.xml`、`devops-agent-dist-*.tar.gz`，已修复 ✅。

---

## 四、已确认的最新文档

以下文档经校对与代码一致：

| 文档 | 状态 |
|------|------|
| `docs-planning/08-development-guide.md` | ✅ 最新（04-29），目录结构、技术栈、表结构均正确 |
| `devops-agent/docs/deployment-guide.md` | ✅ 基本最新，系统要求、部署流程、systemd 均有效 |
| `docs-planning/02-functional-requirements.md` | ✅ 核心功能描述准确，MCP/推理链路状态已更新 |
| `docs-planning/04-data-model.md` | ✅ 数据模型设计文档与代码一致 |
| `docs-planning/01-business-goals.md` | ✅ 业务目标无需因代码变更而修改 |

---

## 五、修复优先级建议

| 优先级 | 文件 | 问题 |
|--------|------|------|
| **P0** | `devops-agent/README.md` | 目录结构全部过时，严重影响新人理解和评审得分 |
| **P0** | `docs-planning/06-tech-stack.md` | 前端选型（Vue→React）根本错误 |
| **P1** | `devops-agent/docs/backend-gap-analysis.md` | P0 功能已实现但标注未完成 |
| **P1** | `docs-planning/07-architecture.mmd` | 架构图前端标注错误 |
| **P1** | `docs-planning/README.md` | 速查表前端技术栈错误 |
| **P2** | `docs-planning/05-api-contract.md` | 缺失新增接口 |
| **P2** | `docs-planning/08-development-guide.md` | 路由数量偏差（非关键） |
| **P3** | `devops-agent/docs/README.md` | 缺失 CI/CD 文件索引 |
| **P3** | `devops-agent/README.md` | 快速开始命令修正 |
